sudo: required
language: generic
services:
  - docker

before_install:
  - docker build -t abdelrhmansafwat/docker-react -f Dockerfile.dev .

script:
  - docker run  -e CI=true abdelrhmansafwat/docker-react npm run test
# deploy:
#   provider: elasticbeanstalk
#   region: 'us-west-2'
#   app: 'docker'
#   env: 'Docker-env'
#   bucket_name: ''
#   bucket_path: 'docker'
#   on:
#     branch: main
#   access_key_id: $AWS_ACCESS_KEY
#   secret_access_key:
#     secure: "$AWS_SECRET_KEY"
